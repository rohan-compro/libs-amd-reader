export default class ProductInfo {
    constructor(
        public projectName: string,
        public designPack: string,
        public numberOfUnits: number,
        public screensPerUnit: number
    ) { }
}